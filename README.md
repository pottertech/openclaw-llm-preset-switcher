# openclaw-llm-preset-switcher
OpenClaw Auto Preset Switcher is a skill that classifies each agent request and selects the most appropriate LLM operating style for the task.  The skill analyzes the user request, assigns a task class, maps the request to the correct agent role, and recommends the safest and most effective model behavior for that type of work.
